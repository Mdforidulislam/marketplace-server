{
  "compilerOptions": {
    "target": "ES2020",                          /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "module": "commonjs",                         /* Specify what module code is generated. */
    "moduleResolution": "node",                   /* Determine how modules are resolved. */
    "esModuleInterop": true,                      /* Enables ES6-style default imports from CommonJS modules. */
    "allowSyntheticDefaultImports": true,         /* Allow default imports from modules without a default export. */
    "skipLibCheck": true,                         /* Skip type checking of declaration files. */
    "forceConsistentCasingInFileNames": true,     /* Ensure that file names are treated case-sensitively. */
    "strict": true,                               /* Enable all strict type-checking options. */
    "outDir": "./dist",                           /* Redirect output structure to the directory. */
    "rootDir": "./src",                           /* Specify the root folder within your source files. */
    "baseUrl": "./",                              /* Base directory to resolve non-relative module names. */
    "paths": {
      "*": ["node_modules/*", "src/types/*"]      /* Define path aliases if necessary. */
    },
    "resolveJsonModule": true,                    /* Allow importing .json files as modules. */
    "declaration": true,                          /* Generates corresponding .d.ts files. */
    "declarationMap": true,                       /* Create sourcemaps for .d.ts files. */
    "sourceMap": true,                            /* Create sourcemaps for JavaScript files. */
    "incremental": true,                          /* Enable incremental compilation for faster rebuilds. */
    "strictNullChecks": true                      /* Ensure null and undefined are not allowed unless explicitly defined. */
  },
  "include": [
    "src/**/*"                                    /* Include all TypeScript files in the src directory. */
  ],
  "exclude": [
    "node_modules",                               /* Exclude node_modules from the compilation. */
    "dist"                                        /* Exclude output directory from being compiled. */
  ]
}
